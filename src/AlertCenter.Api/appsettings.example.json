{
  "// NOTE": "Documents config shape only — no secrets committed (NFR-4, AC-5). Real channel credentials go in user-secrets / environment, never here.",
  "ConnectionStrings": {
    "AlertCenter": "Data Source=alertcenter.db"
  },
  "Feeds": {
    "Sources": [
      { "Source": "reuters", "Url": "https://example.com/reuters/rss" },
      { "Source": "bbc-news", "Url": "http://feeds.bbci.co.uk/news/rss.xml" }
    ]
  },
  "Scheduling": {
    "PollInterval": "00:05:00",
    "DispatchInterval": "00:00:15",
    "Enabled": true
  },
  "Channels": {
    "// Email (real adapter, not wired in MVP)": "SMTP host/port/user/password via env or user-secrets",
    "// Slack (real adapter, not wired in MVP)": "Webhook URL via env or user-secrets"
  }
}
